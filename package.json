{
  "name": "react-validation-form",
  "version": "0.1.7",
  "description": "A form component for React Library, supporting form validation.",
  "main": "./lib/index.js",
  "scripts": {
    "start": "npm run storybook",
    "build": "./node_modules/.bin/rimraf lib && ./node_modules/.bin/babel src --copy-files --source-maps --out-dir lib",
    "test": "./node_modules/mocha/bin/mocha --compilers js:babel-core/register",
    "storybook": "start-storybook -p 6006",
    "build-storybook": "build-storybook",
    "prepare": "npm run build"
  },
  "author": "zhuscat",
  "repository": {
    "type": "git",
    "url": "https://github.com/zhuscat/react-form.git"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.3",
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "@kadira/storybook": "^2.21.0",
    "babel-cli": "^6.24.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "rimraf": "^2.6.1"
  }
}
